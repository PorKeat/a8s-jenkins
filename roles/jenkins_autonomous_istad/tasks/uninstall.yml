---
- name: Stop and disable Jenkins service
  ansible.builtin.service:
    name: "{{ jenkins_service_name }}"
    state: stopped
    enabled: false
  failed_when: false

- name: Remove Jenkins systemd override
  ansible.builtin.file:
    path: "/etc/systemd/system/{{ jenkins_service_name }}.service.d"
    state: absent

- name: Remove Jenkins nginx site symlink
  ansible.builtin.file:
    path: /etc/nginx/sites-enabled/jenkins
    state: absent

- name: Remove Jenkins nginx site definition
  ansible.builtin.file:
    path: /etc/nginx/sites-available/jenkins
    state: absent

- name: Remove generated self-signed Jenkins origin certificate
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ jenkins_reverse_proxy_tls_cert_path }}"
    - "{{ jenkins_reverse_proxy_tls_key_path }}"
  when:
    - jenkins_reverse_proxy_enabled | bool
    - jenkins_reverse_proxy_tls_mode == 'self_signed'

- name: Remove Jenkins package
  ansible.builtin.apt:
    name: jenkins
    state: absent
    purge: true
    autoremove: true
    update_cache: true

- name: Remove Jenkins apt repository
  ansible.builtin.apt_repository:
    repo: "deb [signed-by={{ jenkins_apt_keyring_path }}] {{ jenkins_apt_repository_base_url }} binary/"
    filename: jenkins
    state: absent
    update_cache: true

- name: Remove Jenkins apt keyring
  ansible.builtin.file:
    path: "{{ jenkins_apt_keyring_path }}"
    state: absent

- name: Remove Jenkins home and runtime data
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "{{ jenkins_home }}"
    - /var/cache/jenkins
    - /var/log/jenkins
  when: jenkins_destroy_data | bool

- name: Remove Jenkins Java runtime when requested
  ansible.builtin.apt:
    name: "{{ jenkins_java_package }}"
    state: absent
    autoremove: true
    update_cache: true
  when: jenkins_destroy_java | bool

- name: Remove nginx when requested
  ansible.builtin.apt:
    name: nginx
    state: absent
    purge: true
    autoremove: true
    update_cache: true
  when:
    - jenkins_reverse_proxy_enabled | bool
    - jenkins_reverse_proxy_destroy_package | bool
